@model UserSide.Models.Message
@using Microsoft.AspNetCore.Identity
@inject UserManager<IdentityUser> UserManager

@{
    ViewData["Title"] = "CreateChat";
}



<h4>Contacts</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateChat">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
                <label asp-for="UserName" class="control-label"></label>
                @Html.DropDownList("YourDropDownList", new SelectList(ViewBag.Users), htmlAttributes: new { @class = "form-control", id = "UserChat" })
                @*<select asp-for="UserName"  class="form-control" />*@
                @*@<span asp-validation-for="UserName" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

