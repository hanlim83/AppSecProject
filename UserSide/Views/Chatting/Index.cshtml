@model IEnumerable<UserSide.Models.Chat>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MsgCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserOne)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserTwo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MsgCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserOne)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserTwo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChatID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ChatID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ChatID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
