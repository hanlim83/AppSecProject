@model AdminSide.Areas.PlatformManagement.Models.AWSTestingSubnetFormModel;
@{
    ViewData["Title"] = "Testing Page";
}

<div class="row">
    <div class="col-lg-3">
        <partial name="../_AWSTestingNav.cshtml" />
    </div>
    <div class="col-lg-9" style="margin-top: 25px; margin-bottom: 10%;">
        <h4>Content</h4>
        <div class="row">
            <div class="col-lg-6">
                @if (!String.IsNullOrEmpty(Convert.ToString(ViewData["Exception"])))
                {
                    <div class="alert alert-danger" role="alert">
                        @ViewData["Exception"]
                    </div>
                }
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="IPCIDR"></label>
                        <input asp-for="IPCIDR" placeholder="Enter IP CIDR" class="form-control" pattern="172.30.[0-9]{1,3}.[0-9]{1,3}" />
                        <span asp-validation-for="IPCIDR" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn btn-primary">Create Subnet</button>
                </form>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
