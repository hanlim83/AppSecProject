@model IEnumerable<AdminSide.Models.CategoryDefault>

@{
    ViewData["Title"] = "Index";
}

<!--Edit UI to specify this page is to speed up multi-select when creating competitions-->
<br>
<div class="container">
    <h2>Default Categories</h2>
    <hr />
    <div class="float-right">
        <a asp-action="Create"><button type="button" class="btn btn-primary">New Default Category</button></a>
    </div>
    <br>
    <br>
    <table class="table table-hover">
        <thead class="thead-dark">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CategoryName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CategoryName)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.ID"><input type="button" value="Edit" class="btn btn btn-default"></a>
                        <a asp-action="Delete" asp-route-id="@item.ID"><input type="button" value="Delete" class="btn btn btn-default"></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>