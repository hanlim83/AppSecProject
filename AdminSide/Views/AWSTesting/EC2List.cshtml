@model Amazon.EC2.Model.DescribeInstancesResponse;
@{
    ViewData["Title"] = "Testing Page";
}

<div class="row">
    <div class="col-lg-3">
        <div class="list-group">
            <a asp-area="" asp-controller="AWSTesting" asp-action="S3List" class="list-group-item">S3 - List Buckets</a>
            <a asp-area="" asp-controller="AWSTesting" asp-action="S3Create" class="list-group-item">S3 - Create Bucket</a>
            <a asp-area="" asp-controller="AWSTesting" asp-action="EC2List" class="list-group-item active">EC2 - List Instances</a>
            <a asp-area="" asp-controller="AWSTesting" asp-action="EC2Create" class="list-group-item">EC2 - Create Instance</a>
            <a asp-area="" asp-controller="AWSTesting" asp-action="VPCList" class="list-group-item">VPC - List VPC</a>
            <a asp-area="" asp-controller="AWSTesting" asp-action="SubnetList" class="list-group-item">VPC - List Subnets</a>
            <a asp-area="" asp-controller="AWSTesting" asp-action="SubnetCreate" class="list-group-item">VPC - Create Subnet</a>
            <a asp-area="" asp-controller="AWSTesting" asp-action="CloudwatchView" class="list-group-item">Cloudwatch - View Events</a>
            <a asp-area="" asp-controller="AWSTesting" asp-action="SNSSend" class="list-group-item">SNS - Send SMS</a>
        </div>
    </div>
    <div class="col-lg-9">
        <h4>Content</h4>
        <div class="row">
            <div class="col-lg-6">
                @if (!String.IsNullOrEmpty(Convert.ToString(ViewData["Result"])) && Convert.ToString(ViewData["Result"]) != "Successfully Terminated!" && Convert.ToString(ViewData["Result"]) != "Successfully Created and Running!")
                {
                    <div class="alert alert-danger" role="alert">
                        @ViewData["Result"]
                    </div>
                }
                @if (Convert.ToString(ViewData["Result"]) == "Successfully Created and Running!" || Convert.ToString(ViewData["Result"]) == "Successfully Terminated!")
                {
                    <div class="alert alert-success" role="alert">
                        @ViewData["Result"]
                    </div>
                }
                <table class="table">
                    <thead>
                        <tr>
                            <th>
                                Instance ID
                            </th>
                            <th>
                                Architecture
                            </th>
                            <th>
                                Launch Time
                            </th>
                            <th>
                                Instance State
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            @foreach (var reservation in Model.Reservations)
                            {
                                @foreach (var instance in reservation.Instances)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => instance.InstanceId)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => instance.Architecture)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => instance.LaunchTime)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => instance.State.Name.Value)
                                        </td>
                                        <td>
                                            <form method="post">
                                                <input type="hidden" name="instanceId" value="@instance.InstanceId" />
                                                <button type="submit" class="btn btn-danger">Terminate Instance</button>
                                            </form>

                                        </td>
                                    </tr>
                                }

                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
